<div id="{{key}}" class="message">
  <div>
    <span class="message-name">{{name}}</span>
    {{#if key}}
    <div class="message-delete"><i class="material-icons">delete</i></div>
    {{/if}}
  </div>
  <span class="message-text">{{text}}</span>
</div>